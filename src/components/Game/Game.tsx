import Input from '../Input';
import styles from './Game.module.css';

export const Game = () => {
  return (
    <div className={styles.gameWrapper}>
      <div>

      </div>
      <Input />
    </div>
  );
};
